我们将第三期的核心目标定义为：**赋予 Agent 手脚（MCP）、耳朵（ASR）、嘴巴（TTS）以及联网的实时视野。**

---

# 📑 第三期：全能 Agentic 助理整合执行文档

## 一、 核心架构全景图

系统将由 **“大脑” (Local LLM)** 驱动，通过 **“神经中枢” (FastAPI)** 连接 **“感官” (Vision/Voice)** 和 **“手脚” (MCP Tools)**。

---

## 二、 核心功能模块整合

### 1. 神经中枢：MCP (模型上下文协议)

这是第三期的最大亮点，解决 Agent “不能动”的问题。

- **连接器 (MCP Client)：** 后端集成 MCP 客户端，支持动态挂载各种 Server。

- **首发工具包：**
  
  - **文件系统 (Filesystem)：** 读写本地代码、整理文件夹、导出文档。
  
  - **代码解释器 (Python Interpreter)：** 在沙箱中执行计算、生成图表。
  
  - **实时搜索 (Search)：** 通过 Brave 或 Google 搜取最新资讯，补全本地模型时效性差的短板。

### 2. 交互双翼：多模态语音 (Voice & Vision)

让交互不再局限于键盘。

- **听觉 (ASR)：** 部署 `faster-whisper` 本地模型。实现“语音直达”，识别精准且极速。

- **表达 (TTS)：** 部署 `Edge-TTS` (丝滑、自然) 或 `GPT-SoVITS` (克隆你的声音)。

- **视觉 (Vision)：** 沿用 `qwen3-vl`，配合 MCP 的“屏幕截图”工具，实现实时看屏。

### 3. 进化引擎：RAG + 记忆反思 (Self-Evolution)

- **动态库管理：** 结合 MCP 自动扫描本地文件夹，发现新文档自动入库。

- **智慧提炼：** 定期通过反思任务，将对话中的琐碎信息提炼为 `User Profile` 中的结构化记忆。

---

## 三、 详细技术栈方案 (全本地化方案)

| **模块**         | **推荐技术栈**                        | **理由**                   |
| -------------- | -------------------------------- | ------------------------ |
| **LLM 调度**     | **LangChain / LangGraph**        | 支持复杂的 ReAct 循环和工具调用逻辑    |
| **工具协议**       | **Model Context Protocol (MCP)** | 标准化工具生态，一行命令接入新功能        |
| **语音识别 (ASR)** | **Faster-Whisper**               | 本地运行最稳、最快的语音转文字方案        |
| **语音合成 (TTS)** | **Edge-TTS / Kokoro-82M**        | 声音极其拟人且对资源消耗极低           |
| **实时搜索**       | **Brave Search MCP**             | 隐私保护好，API 接入简单           |
| **代码执行**       | **E2B 或 Docker 沙箱**              | 安全运行 Agent 生成的 Python 代码 |

---

## 四、 三步走执行路径

### 🚩 第一阶段：工具觉醒 (MCP 落地)

1. **环境配置：** 安装 `Node.js` (npx) 和 Python `mcp` SDK。

2. **核心开发：** 实现 `MCPManager` 类，成功让 Gemma/Qwen 调用 `Filesystem` 工具读取桌面文件。

3. **Prompt 升级：** 编写能够稳定触发工具调用的 JSON 格式 Prompt。

### 🚩 第二阶段：感官合一 (语音与多模态)

1. **语音链路：** 前端 Vue 3 增加音频采集，后端接入 `Faster-Whisper` 实现语音提问。

2. **视觉联动：** 实现“看图操作”——如：发一张表格截图，让 Agent 通过 **Python MCP** 把它转成 Excel。

### 🚩 第三阶段：全自主助理 (闭环运行)

1. **联网闭环：** 接入 **Search MCP**，实现“先搜外网，再搜 RAG，最后回答”。

2. **主动反思：** 实现每天定时触发一次“自我总结”，更新长期记忆库。

3. **UI 进化：** Vue 3 界面增加“思考链”展示窗，让用户看到 Agent 正在用什么工具。

---

## 五、 执行代码范例 (多模块协同)

当你在第三期对 Agent 说：**“帮我搜一下今天的大模型新闻，并写一份简报存到桌面”** 时，后端的执行逻辑如下：

```python
async def unified_agent_task(voice_input):
    # 1. 听觉：语音转文字
    text_query = await asr_service.transcribe(voice_input)

    # 2. 思考：大模型决策 (ReAct)
    # Thought: 用户需要最新新闻，我需要先调用 Search MCP 工具。
    search_results = await mcp_client.call_tool("brave_search", {"query": "AI news today"})

    # 3. 行动：调用文件工具
    # Thought: 简报写好了，现在调用 Filesystem MCP 存入桌面。
    file_path = await mcp_client.call_tool("write_file", {
        "path": "C:/Users/Desktop/AI_Daily.md",
        "content": search_results
    })

    # 4. 表达：TTS 语音反馈
    response_text = f"主人，今日新闻已整理完毕，存放在桌面，文件名为 AI_Daily.md"
    audio_output = await tts_service.synthesize(response_text)

    return audio_output # 前端播放音频并显示文件路径
```

---


